# neuroinspo — Python dependencies
# Install with: pip install -r requirements.txt
#
# Used by:
#   process_papers.py    — fetch DOI metadata from CrossRef / arXiv
#   process_superlist.py — fetch and parse the superlab.ca RSS feed

requests>=2.32
